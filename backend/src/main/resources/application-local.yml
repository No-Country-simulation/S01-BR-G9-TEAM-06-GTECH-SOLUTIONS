spring:
  datasource:
    url: "jdbc:mysql://localhost:${DB_PORT:3307}/${DB_NAME:intelliwatts}?preserveInstants=true&connectionTimeZone=SERVER&sslMode=DISABLED&allowPublicKeyRetrieval=true"

server:
  servlet:
    session:
      cookie:
        secure: false
